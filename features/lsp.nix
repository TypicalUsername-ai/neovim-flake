_: {
  config.vim = {
    lsp = {
      enable = true;
      inlayHints.enable = true;
      lspSignature.enable = true;
      lspconfig = {
        enable = true;
      };

    };

    binds.whichKey = {
      enable = true;
    };

  };
}
